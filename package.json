{
	"name": "the-shears-of-atropos",
	"type": "module",
	"scripts": {
		"build": "vite build",
		"dev": "vite",
		"preview": "vite preview",
		"zip": "npm run build && mkdir -p dist && zip -r dist/game.zip dist -x \"**/.DS_Store\""
	},
	"dependencies": {
		"d3-force": "^3.0.0",
		"kaplay": "^3001.0.19"
	},
	"devDependencies": {
		"@types/node": "^25.3.0",
		"vite": "^7.3.1"
	}
}
